Que modificar en la base de datos 

#### tabla para boleteria

CREATE TABLE ventas_entradas_pagos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    idventa INT NOT NULL,
    idmodopago INT NOT NULL,
    importe DECIMAL(10,2) NOT NULL,
    FOREIGN KEY (idventa) REFERENCES ventas_entradas(idventa)
);

### tabla ventas_entradas (Actualizacion importante)

ALTER TABLE ventas_entradas
ADD COLUMN idmodopago INT NULL AFTER cliente;

#Modificar tabla ventas 
ALTER TABLE ventas 
MODIFY estado ENUM('PENDIENTE','OK','CANCELADA') NOT NULL;




